function addNumbers(a, b) {
    const c = a + b
    return c
}

function subtractNumbers(a, b) {
    const c = a - b
    return c
}

function multiplyNumbers(a, b) {
    return a * b
}

module.exports = {
    addNumbers,
    subtractNumbers,
    multiplyNumbers
}